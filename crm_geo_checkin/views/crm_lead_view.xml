<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_crm_lead_form_geo_checkin" model="ir.ui.view">
        <field name="name">crm.lead.form.geo.checkin</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <!-- Añadir pestaña con el botón -->
            <xpath expr="//notebook" position="inside">
                <page string="Geolocalización">
                    <group>
                        <field name="checkin_latitude"/>
                        <field name="checkin_longitude"/>
                        <field name="checkin_datetime"/>
                        <field name="checkin_distance_km"/>
                        <button string="Obtener Ubicación"
                                type="object"
                                class="btn-primary get_location_button"
                                name="get_location_button"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>